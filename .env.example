# Root env for `docker compose`. Copy to `.env` (gitignored) and fill in as needed.
# Every value has a default in docker-compose.yml — the stack runs even if this file is empty.

# ---- Local Postgres (used by the `db` service) ----
POSTGRES_USER=m32
POSTGRES_PASSWORD=m32
POSTGRES_DB=m32

# ---- Database the backend connects to ----
# Leave unset to use the local `db` container above.
# To use Neon instead, paste your pooled connection string and swap the driver to psycopg:
#   postgresql+psycopg://USER:PASSWORD@HOST/DB?sslmode=require
# DATABASE_URL=postgresql+psycopg://...

# ---- Auth ----
JWT_SECRET=dev-insecure-secret-change-me

# ---- LLM (set at least one key; with neither, chat runs in echo mode) ----
# The model is chosen in-app (composer picker); see backend/app/llm/models.py for the catalog.
# Google models — Gemini 3.1 Flash Lite, Gemma 4 31B:
GEMINI_API_KEY=
# OpenAI models — GPT-5.4 Mini, GPT-5.5; setting this also makes GPT-5.5 (low) the default:
OPENAI_API_KEY=

# ---- Google OAuth (Phase 4) ----
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ---- Composio (Phase 3) ----
COMPOSIO_API_KEY=
COMPOSIO_TIMEZONE=UTC

# ---- Frontend ----
VITE_API_BASE_URL=http://localhost:8000
FRONTEND_ORIGIN=http://localhost:5173
